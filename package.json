{
  "name": "travel-planner-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "lucide-react": "^0.364.0"
    // Keep other runtime dependencies here
  },
  "scripts": {
    "start": "webpack serve --mode development --open", // Use webpack-dev-server for development with live reloading and auto-open browser
    "build": "webpack --mode production",             // Correct for production build
    "test": "echo \"No custom test script configured for this example\""
    // Optional: Add a "preview" or "serve-dist" script if you want to quickly test your production build locally
    // "preview": "serve -s dist -l 7860" // Assuming your build output directory is 'dist' (commonly used with Webpack)
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "tailwindcss": "^3.4.3",
    "postcss": "^8.4.38",
    "autoprefixer": "^10.4.19",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.4", // This is explicitly needed now for the "start" script
    "html-webpack-plugin": "^5.6.0",
    "babel-loader": "^9.1.3",
    "@babel/core": "^7.24.4",
    "@babel/preset-env": "^7.24.4",
    "@babel/preset-react": "^7.24.1",
    "css-loader": "^7.1.1",
    "style-loader": "^4.0.0",
    "postcss-loader": "^8.1.1",
    "serve": "^14.2.3", // Keep 'serve' if you intend to use it for local preview of production build
    "copy-webpack-plugin": "^12.0.2",
    "process": "^0.11.10" // Keep if you actually use it in your code for Node.js process environment variables
  }
}
